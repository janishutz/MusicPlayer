{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "properties": {
        "clientMode": {
            "type": "string",
            "enum": [
                "websockets",
                "sse",
                "poll"
            ],
            "default": "poll",
            "description": "Default information retrieval mode for the basic remote displays. Polling is slightly cheaper than websockets at the cost of responsiveness"
        },
        "webUrl": {
            "type": "string",
            "description": "The URL to the frontend. Just the host, in form https://music.janishutz.com"
        },
        "dataDir": {
            "type": "string",
            "default": "./data",
            "description": "The directory in which to store the data (user's playlists)"
        }
    },
    "required": [
        "store",
        "auth",
        "clientMode",
        "webUrl"
    ]
}
