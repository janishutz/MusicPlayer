# Node backend
This backend will not see any more development and is deprecated in its current state.

I ***MAY*** eventually also implement this, but this has VERY low priority, so much so that it is VERY unlikely that that will ever happen
